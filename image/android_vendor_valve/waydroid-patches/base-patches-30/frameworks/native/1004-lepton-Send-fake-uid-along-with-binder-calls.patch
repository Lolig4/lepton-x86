From 23e78f1de1761ed95f408a28e732fe02a87fd23f Mon Sep 17 00:00:00 2001
From: Franz-Josef Haider <frajo@frajo.fi>
Date: Mon, 29 Dec 2025 14:46:42 +0200
Subject: [PATCH 1/1] lepton: Send fake uid along with binder calls.

Change-Id: I4202a2f5182a88cfecb6f482a5515462c5feff2d
---
 libs/binder/IPCThreadState.cpp | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

diff --git a/libs/binder/IPCThreadState.cpp b/libs/binder/IPCThreadState.cpp
index dc3994a20..7bab41126 100644
--- a/libs/binder/IPCThreadState.cpp
+++ b/libs/binder/IPCThreadState.cpp
@@ -1039,7 +1039,7 @@ status_t IPCThreadState::writeTransactionData(int32_t cmd, uint32_t binderFlags,
     tr.target.ptr = 0; /* Don't pass uninitialized stack data to a remote process */
     tr.target.handle = handle;
     tr.code = code;
-    tr.flags = binderFlags;
+    tr.flags = binderFlags | ((getuid() & 0x1ffff) << 8);
     tr.cookie = 0;
     tr.sender_pid = 0;
     tr.sender_euid = 0;
@@ -1195,8 +1195,8 @@ status_t IPCThreadState::executeCommand(int32_t cmd)
 
             mCallingPid = tr.sender_pid;
             mCallingSid = reinterpret_cast<const char*>(tr_secctx.secctx);
-            mCallingUid = tr.sender_euid;
-            mLastTransactionBinderFlags = tr.flags;
+            mCallingUid = (tr.flags >> 8) & 0x1ffff;
+            mLastTransactionBinderFlags = tr.flags & 0xff;
 
             // ALOGI(">>>> TRANSACT from pid %d sid %s uid %d\n", mCallingPid,
             //    (mCallingSid ? mCallingSid : "<N/A>"), mCallingUid);
-- 
2.54.0

