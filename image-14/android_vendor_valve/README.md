# Valve vendor scripts

This directory contains an overlay of patches and manifests that is intended to further patch the Waydroid vendor scripts directory.
These changes trim out pieces of the LineageOS build that are not necessary for Lepton, and also add necessary functionality, such as the process observer that notifies the outer world that the target app has closed.
